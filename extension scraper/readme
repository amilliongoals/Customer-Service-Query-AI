This is a Google Chrome extension being created to scrape, name, summarize, tokenize, and store text from webpages that is highlighted to be used for training. 

User will select where text is from with general cleaning principles pre-selected for each. 
